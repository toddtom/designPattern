状态模式：
允许一个对象在其内部状态改变时改变它的行为。对象看起来似乎修改了它的类。

创建和销毁状态对象：（三种方式,根据实际情况选择）
1.当需要使用状态对象的时候创建，使用完后就销毁它们。(状态不经常改变的情况)
2.提前创建它们并始终不销毁(状态经常改变，并且存储着大量的数据信息)
3.采用延迟记载和缓存合用的方式，就是当第一次需要使用状态对象的时候创建，
使用完后并不销毁对象，而是把这个对象缓存起来，等待下一次使用，而且在合适
的时候，会由缓存框架销毁对象

状态模式的本质：格局状态来分离和选择行为


